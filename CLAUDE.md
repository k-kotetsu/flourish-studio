# Flourish Studio

自分の「ありたい姿」を軸に、仕事・お金・からだ・人との関係を人生全体としてつなげ、育てていくことを支援するウェルビーイングサービス。

**開発は `docs/12_開発計画/backlog.md` のタスク単位で進める。** 各タスクに参照すべきドキュメントが明記してある。**指示されていないドキュメントを先回りして読まない。**

---

## 破ってはいけない規則

1. **評価しない。** 点数・優劣・順位をユーザーに見せない。状態は「段階」で示す
2. **ユーザーの言葉を消さない。** 入力を勝手に言い換えない。エラーや戻る操作で入力を失わせない
3. **売らない。** 商品・サービス・広告への誘導を、有料無料を問わず一切書かない
4. **入力途中を保存しない。** 確定した成果物だけをサーバに送る（`03_ユーザーフロー` 2章）
5. **AIの生成に自動リトライしない。** 失敗はユーザーに見せ、押されたときだけ再実行する
6. **定型文でフォールバックしない。** AI生成が失敗したら、それは失敗として扱う
7. **使わない語：** 診断／未完成／空欄／未入力／頑張っていますね／素晴らしい

---

## スキル

作業の種類に応じて呼ぶ。**ドキュメント全文を読む前に、まず対応するスキルを見る。**

| スキル | いつ |
|---|---|
| `flourish-tone` | ユーザーに見える文章を書くとき（UI文言、エラー文、AIプロンプト） |
| `flourish-ui` | Vueコンポーネント・CSSを書くとき |
| `flourish-data` | DynamoDBに触れるとき |
| `flourish-api` | エンドポイントを追加・変更するとき |
| `flourish-ai` | Bedrock呼び出し・プロンプトを実装するとき |

---

## ドキュメントの地図

| 知りたいこと | 場所 |
|---|---|
| なぜこの製品が存在するか | `docs/01_全体コンセプト/` |
| MVPに含む／含まない | `docs/02_MVP範囲定義/` |
| 画面の遷移と保存タイミング | `docs/03_ユーザーフロー/` |
| 画面ID（S-11 など）の仕様 | `docs/04_画面設計/screen-list.md` |
| 質問文・選択肢・AI出力の中身 | `docs/05_質問・コンテンツ設計/` |
| 画面のレイアウト | `docs/06_ワイヤーフレーム/` |
| 色・寸法・トーン・モーション | `docs/07_デザイン原則/` |
| DynamoDBのキー設計 | `docs/08_データモデル/` |
| エンドポイント仕様 | `docs/09_API設計/` |
| プロンプト全文と出力スキーマ | `docs/10_AIプロンプト設計/` |
| AWS構成・コスト | `docs/11_技術構成/` |
| タスクと進め方 | `docs/12_開発計画/` |

**画面IDは全ドキュメント共通の索引になっている。** `S-16` を実装するなら、`grep -rn "S-16" docs/` で関係箇所が揃う。

---

## 技術スタック

| 層 | 技術 |
|---|---|
| フロント | Vue 3 ＋ Vite ＋ TypeScript ＋ Pinia。UIライブラリは使わない |
| API | FastAPI ＋ Lambda Web Adapter（コンテナ） |
| DB | DynamoDB（単一テーブル `flourish` ＋ `flourish_article`） |
| AI | Bedrock ＋ `anthropic.claude-sonnet-5`（Anthropic SDK の Mantle クライアント） |
| 認証 | Cognito（**トークンをブラウザに渡さない**。BFF方式） |
| IaC | AWS CDK（TypeScript） |

**VPCはない。** Lambdaは非VPCで動く。

---

## コマンド

```bash
make dev          # DynamoDB Local ＋ API ＋ フロントを起動
make test         # ユニット＋統合
make lint         # ruff / eslint / mypy / vue-tsc
make deploy-dev   # dev環境へデプロイ
```

---

## 作業の進め方

- **1タスク＝1セッション**を目安にする。バックログのタスクIDを最初に宣言する
- 実装後は必ず `make lint && make test` を通す
- **仕様に書かれていないことを勝手に決めない。** 判断が要るときは止めて聞く
- 仕様の矛盾を見つけたら、実装を進める前に報告する
